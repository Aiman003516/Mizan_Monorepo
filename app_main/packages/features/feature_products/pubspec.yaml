name: feature_products
description: Manages products, categories, and inventory logic.
publish_to: 'none'

environment:
  sdk: ^3.0.0 # Adjusted for broader compatibility

dependencies:
  supabase_flutter: ^2.5.4
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  drift: ^2.29.0
  image_picker: ^1.1.2
  
  # ✅ NEW: Import Engines
  csv: ^6.0.0
  excel: ^4.0.6
  file_picker: ^8.1.4
  path: ^1.9.0
  
  # Local Dependencies
  core_database:
    path: ../../core/core_database
  core_data:
    path: ../../core/core_data
  core_l10n:
    path: ../../core/core_l10n
  core_ui:
    path: ../../core/core_ui
  shared_ui:
    path: ../../shared/shared_ui
  shared_services:
    path: ../../shared/shared_services

dev_dependencies:
  supabase_flutter: ^2.5.4
  flutter_test:
    sdk: flutter