name: feature_contacts
description: "Contacts and Debts feature for Mizan."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.10.1 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.1
  
  # Internal Packages
  core_data:
    path: ../../core/core_data
  core_ui:
    path: ../../core/core_ui
  core_l10n:
    path: ../../core/core_l10n
  shared_ui:
    path: ../../shared/shared_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
