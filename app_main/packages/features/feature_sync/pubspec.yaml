name: feature_sync
description: Manages data backup and restore logic with Google Drive.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  googleapis: 15.0.0
  path: ^1.9.1
  path_provider: ^2.1.5

  # Local Dependencies
  core_database:
    path: ../../core/core_database

# --- Architecture Modules ---
  core_data:
    path: ../../core/core_data
    
  # --- Cloud Capabilities ---
  cloud_firestore: ^5.0.0  # <--- NEW: The Sync Engine needs this
  firebase_auth: ^5.0.0    # <--- NEW: To know WHO is syncing    
  
  # Feature Dependencies
  feature_auth:
    path: ../feature_auth

dev_dependencies:
  flutter_test:
    sdk: flutter