name: feature_settings
description: Manages app settings (profile, security, currencies) and UI.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  drift: ^2.29.0
  local_auth: ^2.3.0
  url_launcher: ^6.3.2
  share_plus: ^10.0.0
  firebase_auth: ^5.7.0
  
  # Local Dependencies
  core_database:
    path: ../../core/core_database
  core_data:
    path: ../../core/core_data
  core_l10n:
    path: ../../core/core_l10n
  
  # 🟢 FIX: Point to the local folder, not the internet
  shared_ui: 
    path: ../../shared/shared_ui

  # Feature Dependencies
  feature_sync:
    path: ../feature_sync

dev_dependencies:
  flutter_test:
    sdk: flutter