name: feature_settings
description: Manages app settings (profile, security, currencies) and UI.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  drift: ^2.29.0
  local_auth: ^2.3.0
  url_launcher: ^6.3.2
  share_plus: ^10.0.0  # 👈 ADD THIS
  
  # Local Dependencies
  core_database:
    path: ../../core/core_database
  core_data:
    path: ../../core/core_data
  core_l10n:
    path: ../../core/core_l10n
  
  # Feature Dependencies
  # We will create these soon. This error is expected.
  feature_sync:
    path: ../feature_sync
  firebase_auth: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter