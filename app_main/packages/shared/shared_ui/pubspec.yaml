name: shared_ui
description: Shared UI widgets for the Mizan monorepo.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  # ⚠️ FIXED: aligned with the rest of the monorepo to avoid conflicts
  flutter_riverpod: ^2.6.1

  # Local Dependencies
  core_l10n:
    path: ../../core/core_l10n
  core_data: # 👈 ADDED THIS
    path: ../../core/core_data

dev_dependencies:
  flutter_test:
    sdk: flutter